-if (Time.now - comment.created_at) <= 60 * 60
  = ((Time.now - comment.created_at) / 60).to_i.to_s + "分前"
-elsif (Time.now - comment.created_at) <= 60 * 60 * 24
  = ((Time.now - comment.created_at) / 3600).to_i.to_s + "時間前"
-elsif (Date.today - comment.created_at.to_date) <= 30
  = (Date.today - comment.created_at.to_date).to_i.to_s + "日前"
-else
  = comment.created_at